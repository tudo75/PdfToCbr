# install icon
install_data(
  'pdftocbr.svg',
  install_dir: join_paths(
        get_option('datadir'), 
        'icons', 
        'hicolor',
        'scalable',
        'apps',
    ),
)

# install launcher
install_data(
  'pdftocbr.desktop',
  install_dir: join_paths(
        get_option('datadir'), 
        'applications',
    ),
)